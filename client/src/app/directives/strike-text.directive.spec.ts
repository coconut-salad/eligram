import { StrikeTextDirective } from './strike-text.directive';

describe('StrikeTextDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikeTextDirective();
    expect(directive).toBeTruthy();
  });
});
